import random
def roll():
    min_value = 1
    max_value = 6
    return random.randint(min_value, max_value)
    return roll()

while True:
    players = input("Enter the number of players (2-4): ")
    if players.isdigit():
        players = int(players)
        if 1 < players <= 4:
            break
        else:
            print("Invalid input. Please enter a number between 2 and 4.")  
    else:
        print("Invalid input. Please enter a valid number.")

max_score = 15
players_scores = [0 for i in range(players)]
current_score = 0
while max(players_scores) < max_score:
    for players in range(players):
        print((f'Player {players+ 1}s turn. your score:{current_score}'))
            current_score = 0
while True:
    should_roll=input("Do you want to roll the dice? (yes/no): ").lower()
        if should_roll != 'yes' or should_roll != 'y':
            break
        value = roll()
            if value == 1:
                print("You rolled a 1! Your score resets to 0.")
                current_score = 0
            else:
                value = roll()
                    current_score += value
                    print(f"You rolled a {value}. Your current score is {current_score}.")
                players_scores[players] += current_score
                print('your current score is', players_scores[players])
else:
    print(f"Player {players + 1} wins with a score of {players_scores[players]}!")